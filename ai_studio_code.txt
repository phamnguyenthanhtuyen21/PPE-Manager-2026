{editingDeliveryId && (
  <button
    type="button"
    onClick={() => {
      setEditingDeliveryId(null);
      // Reset form về mặc định...
    }}
    className="px-3 py-1 bg-amber-500 hover:bg-amber-600 rounded text-white text-[11px] font-black cursor-pointer"
  >
    HỦY CHỈNH SỬA
  </button>
)}