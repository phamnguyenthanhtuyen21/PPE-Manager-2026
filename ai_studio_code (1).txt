<button
  onClick={() => {
    setEditingDeliveryId(del.id);
    setDelForm({
      delivery_date: del.delivery_date,
      delivery_note_no: del.delivery_note_no,
      project: del.project,
      supplier: del.supplier,
      note: del.note || "",
      items: del.items || [],
      // Nạp đầy đủ thông tin khác...
    });
    window.scrollTo({ top: 0, behavior: "smooth" }); // Cuộn mượt lên form nhập liệu
  }}
  className="p-1 px-1.5 text-sky-600 hover:bg-sky-50 rounded font-bold"
>
  ✏️ Sửa
</button>